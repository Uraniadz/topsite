import сommon from './en/common.json';

export type Resources = {
  common: typeof сommon;
  // home: typeof home;
  // about: typeof about;
  // price: typeof price;
  // services: typeof services;
};
